# Bundler の一時ファイル（依存管理ツール）
/.bundle/

# ログ・一時ファイル（.keep を除外して保持）
/log/
!/log/.keep
/log/*.log

/tmp/
!/tmp/.keep
/tmp/pids/
!/tmp/pids/.keep
/tmp/storage/
!/tmp/storage/.keep

# 開発環境でのアップロードファイル（ActiveStorage など）
/storage/
!/storage/.keep

# ビルド生成物（プリコンパイル済みの assets）
/public/assets
/public/packs
/public/packs-test

# Rails の機密ファイル（credentials や master.key など）
/config/master.key
/config/credentials.yml.enc
/config/credentials/production.key
/config/credentials/*.key
/config/credentials/*.yml.enc

# 環境変数ファイル（dotenv 系）
.env
.env.*

# credentials のバックアップファイルを除外
/backup/*
*.bak
*.old
*.older
config/**/*.bak
config/**/*.old
config/**/*.older

# SQLite や DB のダンプファイル
/db/*.sqlite3
/db/*.sqlite3-journal
*.dump
*.dump.*
*.sql
/latest.dump*
/backup_before_squash.sql

# Node.js や Yarn 関連の依存ファイル
/node_modules/
yarn-error.log

# Webpacker の出力ファイル
/app/assets/builds/
!/app/assets/builds/.keep

# vendor 配下の Bundler 結果
/vendor/bundle/

# エディタや OS による一時ファイル
*.swp
*.swo
.DS_Store
.vscode/

# デバッグ履歴・テストカバレッジレポートなど
.byebug_history
.pry_history
coverage/
/console_debug.log
/credentials_backup.txt

# アップロードされたファイル（旧 CarrierWave などに対応）
/public/system/
/public/uploads/

# GitHub Actions や CI 設定ファイル（必要に応じて管理対象に）
!.github/
!.reviewdog.yml
!.rubocop.yml
